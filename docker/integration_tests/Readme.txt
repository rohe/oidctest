You can use this Docker configuration to run integration tests locally. 
This container is not included in ../docker-compose.yml and needs to be built and 
started manually. 

You can use "make" to build, start, stop... the container. Run "make" from this 
folder without any arguments to see a list of available options.